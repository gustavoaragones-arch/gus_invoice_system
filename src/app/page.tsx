/**
 * Placeholder root page. Phase 3 delivers the application foundation and
 * persistence layer only — no product UI. See docs/phase-3 for what was
 * built and docs/phase-0..2 for the approved requirements/architecture
 * this implementation satisfies.
 */
export default function HomePage() {
  return (
    <main>
      <h1>Canadian Private Billing System</h1>
      <p>
        Application foundation (Phase 3). Product UI is not part of this
        phase — see <code>docs/phase-3</code> for implementation notes.
      </p>
    </main>
  );
}
